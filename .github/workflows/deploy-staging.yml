name: Deploy to Staging

on:
  push:
    branches:
      - develop

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Vercel staging deployment
        run: |
          echo "Triggering deploy hook..."
          curl -v -X POST --data "" "${{ secrets.VERCEL_STAGING_DEPLOY_HOOK }}"
